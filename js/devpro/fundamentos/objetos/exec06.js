// Verificando se uma propriedade existe
// Verifique se o objeto pessoa2 tem a propriedade profissao

const pessoa = { nome: 'Mob', idade: 20, profissao: 'Carpinteiro' };

console.log("profissao" in pessoa)