// Criando a função e passando o parâmetro
function stringc(palavra){
    return palavra.toUpperCase()
}

// Printando no console
console.log(stringc("Testando a função que converte texto para caixa alta."));