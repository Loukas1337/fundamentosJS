// Criando um objeto simples
// Crie um objeto chamado pessoa com as propriedades nome, idade e cidade. Depois, exiba essas propriedades no console.

const pessoa = {
    nome: "Mob",
    idade: 20,
    cidade: "Japan",
};

console.log(pessoa.nome)
console.log(pessoa.idade)
console.log(pessoa.cidade)